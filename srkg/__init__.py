"""SR knowledge graph generation package.

The package is organized as a small DAG of generation stages: configuration,
data validation, edge semantics, layout, PyVis rendering, HTML injection, and
the high-level pipeline that coordinates those pieces.
"""
